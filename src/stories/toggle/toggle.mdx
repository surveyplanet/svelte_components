import {
	Meta,
	Canvas,
	ColorPalette,
	ColorItem,
	Description,
	Story,
} from '@storybook/addon-docs';

import { Toggle } from '../../lib';

<Meta
	title="Form Inputs/Toggle"
	component={Toggle}
/>

# Toggle component

<div id="example">
	<Story id="form-inputs-toggle--default" />
</div>

## Usage

```ts
<script>
	import {Toggle} from '@surveyplanet/svelte-components';
	function changeEventHandler (on) {
		if (on) {
			console.log('Toggle ON 🎉');
		} else {
			console.log('Toggle OFF 😤');
		}
	}
</script>

<Toggle on disabled tall on:change={changeEventHandler}></Toggle>
```

## Properties

| Name      | Type    | Description                                                  |
| --------- | ------- | ------------------------------------------------------------ |
| on        | boolean | Whether the toggle is on or not                              |
| disabeled | boolean | Whether the toggle is disabled or not                        |
| tall      | boolean | Whether the toggle should appear slightly taller than normal |

## Events

| Name   | Return type | Description                                                                            |
| ------ | ----------- | -------------------------------------------------------------------------------------- |
| change | Boolean     | When the toggle is clicked the 'change' event is dispatched along with the on property |
