import { Meta } from '@storybook/addon-docs';

<Meta title="Utilities/Validate" />

# Validate utility

## Usage

```html
<script>
	const inputEl = getElementById('email');
	function submit() {
		const errors = validate(inputEl);
		if (errors) {
			return alert(errors[0].error);
		}
	}
</script>

<label for="email">Email</label>
<input
	type="email"
	id="email"
	name="email"
	data-validate-rules="require,email"
	data-validate-message="My custom validation message (or just use the default)"
	data-validate-hide-errors />
```

## Parameters

| Name    | Type   | Description                    |
| ------- | ------ | ------------------------------ |
| `input` | string | The input element to validate. |
